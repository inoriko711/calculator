import React from 'react';
import ReactDOM from 'react-dom';
import CalContainer from './containers/CalculatorContainer';


// ========================================

ReactDOM.render(
    <CalContainer />,
    document.getElementById('root')
);
